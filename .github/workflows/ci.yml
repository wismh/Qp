name: CI

on:
  pull_request:
  push:
    branches: [dev, main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Test
        run: ctest --test-dir build -C Release --output-on-failure --parallel

  bench:
    name: bench
    runs-on: ubuntu-latest
    permissions:
      contents: write
      deployments: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel --target qplus_bench

      - name: Run benches
        run: |
          mkdir -p build
          ./build/qplus_bench \
            --benchmark_min_time=0.1s \
            --benchmark_out=build/bench.json \
            --benchmark_out_format=json

      - uses: actions/upload-artifact@v4
        with:
          name: qplus-bench-${{ github.sha }}
          path: build/bench.json
          if-no-files-found: error

      # github-action-benchmark fetches gh-pages for the previous series.
      - name: Ensure gh-pages branch
        env:
          GIT_AUTHOR_NAME: github-actions[bot]
          GIT_AUTHOR_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com
          GIT_COMMITTER_NAME: github-actions[bot]
          GIT_COMMITTER_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com
        run: |
          set -euo pipefail
          if git ls-remote --exit-code --heads origin gh-pages >/dev/null; then
            echo "gh-pages already exists"
            exit 0
          fi
          empty="$(git hash-object -t tree --stdin </dev/null)"
          commit="$(git commit-tree "${empty}" -m "chore: bootstrap gh-pages for bench history")"
          git push origin "${commit}:refs/heads/gh-pages"

      # Compare against the last main result. Do not push history from PRs.
      - name: Comment bench comparison
        if: github.event_name == 'pull_request'
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: qplus_bench
          tool: googlecpp
          output-file-path: build/bench.json
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: false
          save-data-file: false
          comment-always: true
          summary-always: true
          alert-threshold: "200%"
          comment-on-alert: true
          fail-on-alert: false

      # Persist the series on gh-pages after landings on main.
      - name: Store bench result
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: qplus_bench
          tool: googlecpp
          output-file-path: build/bench.json
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: true
          summary-always: true
          alert-threshold: "200%"
          comment-on-alert: true
          fail-on-alert: false
          max-items-in-chart: 50
