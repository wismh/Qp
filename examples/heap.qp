pub struct Point {
    mut x: i32,
    mut y: i32,
}

pub fn origin() -> Point? {
    new Point { x: 0, y: 0 }
}

pub fn make(x: i32, y: i32) -> Point? {
    new Point { x: x, y: y }
}

pub fn get_x(p: Point?) -> i32 {
    if p == null {
        0
    } else {
        p!.x
    }
}
