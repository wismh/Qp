pub fn run() -> i32 {
    let h = sqrt(3.0 * 3.0 + 4.0 * 4.0);
    let m = fmod(5.0, 2.0);
    let p = pow(2.0, 3.0);
    let s = sin(0.0);
    let c = cos(0.0);
    if h == 5.0 && m == 1.0 && p == 8.0 && s == 0.0 && c == 1.0 {
        1
    } else {
        0
    }
}
